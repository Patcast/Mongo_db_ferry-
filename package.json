{
  "name": "ledger-memory",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Judgment memory for an accounting practice — onboarding that re-prices itself when a human writes a verdict.",
  "scripts": {
    "dev": "node --env-file-if-exists=.env src/server.js",
    "seed": "node --env-file-if-exists=.env scripts/seed.js",
    "reset": "node --env-file-if-exists=.env scripts/reset.js",
    "check": "node --env-file-if-exists=.env scripts/check.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "mongodb": "^6.9.0"
  }
}
